## 🗺️ **Карта JSON вакансии HeadHunter (hh.ru)**

> ✅ Основано на актуальном ответе API (август 2025).  
> 📌 Поля сгруппированы по смыслу.  
> ⚠️ Указаны типы данных и возможные значения.

---

### 1. 🆔 Основная информация

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `id` | string | Уникальный ID вакансии. Пример: `"124021845"` |
| `premium` | boolean | Является ли вакансия платной (рекламной). Пример: `false` |
| `name` | string | Название вакансии. Пример: `"Программист-стажер 1С"` |
| `closed_for_applicants` | boolean | Закрыта ли вакансия для откликов. Пример: `false` |
| `archived` | boolean | Вакансия в архиве. Пример: `false` |
| `hidden` | boolean | Скрыта ли вакансия от публичного поиска. Пример: `false` |
| `approved` | boolean | Проверена модератором. Пример: `true` |

---

### 2. 📍 Местоположение и адрес

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `area` | object | Регион/город |
| `area.id` | string | ID региона (справочник). Пример: `"41"` — Калининград |
| `area.name` | string | Название города. Пример: `"Калининград"` |
| `area.url` | string | Ссылка на API региона |
| `address` | object/null | Адрес офиса |
| `address.city` | string | Город |
| `address.street` | string | Улица |
| `address.building` | string | Номер здания |
| `address.lat`, `lng` | number | Координаты (широта, долгота) |
| `address.raw` | string | Полный адрес одной строкой |
| `address.metro` | object/null | Ближайшее метро (если есть) |
| `address.metro_stations` | array | Список станций метро рядом |

---

### 3. 💰 Зарплата

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `salary` | object/null | Диапазон зарплаты |
| `salary.from` | number/null | Минимальная зарплата. Пример: `45000` |
| `salary.to` | number/null | Максимальная зарплата. Пример: `90000` |
| `salary.currency` | string | Валюта: `"RUR"`, `"USD"`, `"EUR"` |
| `salary.gross` | boolean | `true` — до вычета налогов, `false` — на руки |
| `salary_range` | object | Расширенная информация о зарплате (аналогично `salary`, но с mode/frequency) |

---

### 4. 🕒 График и формат работы

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `schedule` | object | График работы |
| `schedule.id` | string | `fullDay`, `remote`, `flexible`, `shift`, `flyInFlyOut` |
| `schedule.name` | string | "Полный день", "Удалённая работа" и т.д. |
| `employment` | object | Тип занятости |
| `employment.id` | string | `full`, `part`, `project`, `volunteer`, `probation` |
| `employment.name` | string | "Полная занятость", "Частичная" и т.д. |
| `employment_form` | object | Форма занятости (дублирует `employment`, но детальнее) |
| `employment_form.id` | string | `FULL`, `PART`, `PROJECT`, `VOLUNTEER` |
| `employment_form.name` | string | "Полная", "Проектная" и т.д. |
| `work_format` | array | Формат работы: `ON_SITE`, `REMOTE`, `HYBRID` |
| `working_days` | array | Дни работы (например, `["mon", "tue"]`) — редко |
| `working_time_intervals` | array | Интервалы рабочего времени — редко |
| `working_time_modes` | array | Режимы времени (например, "сменный") |
| `work_schedule_by_days` | array | Расписание по дням: `FIVE_ON_TWO_OFF`, `OTHER` |
| `working_hours` | array | Продолжительность рабочего дня: `HOURS_8`, `HOURS_9`, `OTHER` |
| `night_shifts` | boolean | Есть ли ночные смены |
| `accept_temporary` | boolean | Принимаются временные работники |
| `fly_in_fly_out_duration` | array | Длительность вахты (если `flyInFlyOut`) |

---

### 5. 👤 Опыт и квалификация

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `experience` | object | Требуемый опыт |
| `experience.id` | string | `noExperience`, `between1And3`, `between3And6`, `moreThan6` |
| `experience.name` | string | "Нет опыта", "От 1 года до 3 лет" и т.д. |
| `professional_roles` | array | Профессиональные роли |
| `professional_roles[0].id` | string | `96` — Программист |
| `professional_roles[0].name` | string | "Программист, разработчик" |
| `key_skills` | array | Ключевые навыки (если указаны) |
| `accept_incomplete_resumes` | boolean | Можно ли откликаться с незавершённым резюме |
| `accept_handicapped` | boolean | Принимаются кандидаты с инвалидностью |
| `accept_kids` | boolean | Можно ли устраивать несовершеннолетних |
| `age_restriction` | object/null | Ограничение по возрасту (если есть) |

---

### 6. 🏢 Работодатель

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `employer` | object | Информация о компании |
| `employer.id` | string | ID работодателя |
| `employer.name` | string | Название компании. Пример: `"ЭКСПРЕСС"` |
| `employer.url` | string | Ссылка на API компании |
| `employer.alternate_url` | string | Ссылка на страницу компании на HH |
| `employer.logo_urls` | object/null | Логотипы компании: `original`, `90`, `240` — размеры |
| `employer.vacancies_url` | string | Ссылка на все вакансии компании |
| `employer.accredited_it_employer` | boolean | Аккредитованный IT-работодатель |
| `employer.trusted` | boolean | Проверенный работодатель |

---

### 7. 📝 Описание вакансии

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `description` | string (HTML) | Полное описание вакансии (включая обязанности, требования, условия) |
| `branded_description` | string/null | Брендированное описание (если есть) |
| `vacancy_constructor_template` | object/null | Шаблон конструктора вакансий |
| `snippet` | object/null | Краткое описание (в других вакансиях есть, здесь — в `description`) |
| `snippet.requirement` | string | Требования к кандидату |
| `snippet.responsibility` | string | Обязанности |

> ⚠️ В этом JSON `snippet` отсутствует — вся информация в `description`.

---

### 8. 📅 Даты

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `published_at` | string (ISO 8601) | Дата публикации. Пример: `"2025-08-19T16:24:09+0300"` |
| `created_at` | string | Дата создания |
| `initial_created_at` | string | Дата первого создания (если вакансия обновлялась) |

---

### 9. 🔗 Ссылки и отклики

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `url` | string | Ссылка на API вакансии |
| `alternate_url` | string | Ссылка на страницу вакансии на сайте HH |
| `apply_alternate_url` | string | Ссылка для отклика |
| `response_url` | string/null | Внешняя ссылка для отклика (если указана) |
| `negotiations_url` | string/null | Ссылка на переговоры (для работодателя) |
| `suitable_resumes_url` | string/null | Ссылка на подходящие резюме |
| `allow_messages` | boolean | Можно ли писать сообщения работодателю |

---

### 10. 🧩 Дополнительные поля

| Поле | Тип | Описание / Пример |
|------|-----|------------------|
| `type` | object | Тип вакансии |
| `type.id` | string | `open`, `closed`, `hidden` |
| `type.name` | string | "Открытая" |
| `relations` | array | Связанные объекты (например, приглашения) |
| `department` | object/null | Подразделение в компании |
| `code` | string/null | Внутренний код вакансии |
| `quick_responses_allowed` | boolean | Разрешены быстрые ответы |
| `driver_license_types` | array | Требуемые категории водительских прав |
| `languages` | array | Требуемые языки |
| `has_test` | boolean | Есть ли тестовое задание |
| `test` | object/null | Данные о тесте (если есть) |
| `billing_type` | object | Тип размещения (тариф) |
| `billing_type.id` | string | `standard_plus`, `premium` и т.д. |
| `billing_type.name` | string | "Стандарт плюс" |
| `insider_interview` | object/null | Информация о "Insider Interview" |
| `response_letter_required` | boolean | Требуется ли сопроводительное письмо |
| `show_contacts` | boolean | Показывать ли контакты работодателя |
| `show_logo_in_search` | boolean/null | Показывать ли логотип в поиске |

---

## 📌 Что из этого полезно извлекать?

Если ты парсишь вакансии, чаще всего интересуют:

```python
{
    "id": vacancy["id"],
    "title": vacancy["name"],
    "company": vacancy["employer"]["name"],
    "city": vacancy["area"]["name"],
    "salary_from": vacancy.get("salary", {}).get("from"),
    "salary_to": vacancy.get("salary", {}).get("to"),
    "currency": vacancy.get("salary", {}).get("currency"),
    "gross": vacancy.get("salary", {}).get("gross"),
    "experience": vacancy["experience"]["name"],
    "employment": vacancy["employment"]["name"],
    "schedule": vacancy["schedule"]["name"],
    "work_format": [wf["name"] for wf in vacancy.get("work_format", [])],
    "requirements": extract_from_description(vacancy["description"], "Требования"),  # можно парсить HTML
    "responsibilities": extract_from_description(vacancy["description"], "Обязанности"),
    "conditions": extract_from_description(vacancy["description"], "Условия"),
    "published_at": vacancy["published_at"],
    "url": vacancy["alternate_url"].strip(),
    "description": vacancy["description"]  # или очищенный текст
}
```

---

## 💡 Советы по парсингу

1. **Очищай строки**: `.strip()` у `alternate_url`, `name` и т.д.
2. **Парси HTML в `description`** с помощью `BeautifulSoup`, если нужно вытащить "Требования", "Обязанности" отдельно.
3. **Проверяй `null`**: используй `.get("key", default)` вместо прямого доступа.
4. **Кодировка CSV**: используй `utf-8-sig`, чтобы Excel корректно открывал русский текст.
